language: python
sudo: false
python:
    - 2.7

addons:
  apt:
    packages:
      - libhdf5-serial-dev
      - netcdf-bin
      - libnetcdf-dev

install:
    - pip install netCDF4 nose
script: nosetests
