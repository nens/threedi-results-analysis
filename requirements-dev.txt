# test deps
mock
pytest
pytest-cov
coverage
pycodestyle
flake8

# misc required deps that are not included with QGIS on linux
# numpy and cython may be needed for building netCDF4 (see netcdf4-python)
numpy
cython

future
# you can attempt to try a newer version, but this works for me on QGIS 2.14 + Ubuntu 14.04
# h5py==2.5.0
